# Archiv (nur historisch)

Dieser Ordner enthält **keine** aktiven Installations- oder Migrationsdateien mehr.

**Aktuell gültig:** Nur **`documentation/install_neuinstallation_complete.sql`** (phpMyAdmin, leere DB auswählen) bzw. **`include/sql.sql`** (Vollschema inkl. optional `CREATE DATABASE`).

Hier liegen nur noch:

- **`sql_legacy/`** – frühere Einzel-Patches, `MASTER_MIGRATION_A20.sql`, alte Hilfs-SQLs (`Drop.sql`, `sql2.sql`). Nicht für neue Projekte vorgesehen.
- **A11 / A17 / A18 / A19 / A21** – alte Notizen, Checklisten und Installationsanleitung (Windows/bplaced mit Migrationspfad).

Wenn du **keine** Alt-Datenbank mehr pflegst, kannst du diesen Ordner auch komplett löschen – das laufende System braucht ihn nicht.
